{
  "name": "prototype",
  "version": "1.0.0",
  "description": "prototype of cs160 project",
  "main": "server.js",
  "scripts": {
    "client-install": "npm install --prefix frontend",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm server.js",
    "server": "nodemon server.js",
    "client": "npm start --prefix frontend",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  },
  "author": "Stan",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^5.3.0",
    "express": "^4.17.1",
    "mongoose": "^5.10.6"
  },
  "devDependencies": {
    "nodemon": "^2.0.4"
  }
}
